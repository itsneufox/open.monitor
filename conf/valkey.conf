

# Basic settings
bind 0.0.0.0
port 6379

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (disable for cache-only usage)
save ""
appendonly no

# Performance
tcp-keepalive 60
timeout 300

# Logging
loglevel notice